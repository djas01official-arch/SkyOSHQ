/** @type {import("prettier").Config} */
const config = {
  arrowParens: 'always',
  endOfLine: 'lf',
  printWidth: 100,
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
};

export default config;
